export interface RepliesProps {
  replies: {
    img: string;
    isStaff: boolean;
    name: string;
    number: number;
  };
}
export interface TableTitleProps {
  title: string;
  category: string;
}
